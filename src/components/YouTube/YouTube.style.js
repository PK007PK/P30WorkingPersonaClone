import styled from "styled-components"

export const StyledWrapper = styled.div`
  iframe {
    max-width: 100%;

    :hover {
      position: absolute;
      width: 100%;
      height: 700px;
      max-height: 65vh;
      top: 0;
      left: 0;
    }
  }
`
